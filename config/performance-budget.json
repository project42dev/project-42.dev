{
  "$schemaVersion": 1,
  "measuredOn": "2026-09-06",
  "measuredAt": "portal v0.19.0, platform v0.104.0, theme 06-galactic-guide, Gallery 011fd08",
  "note": "Budgets are set from what the build actually produces today plus a stated margin. A budget nobody can meet gets disabled, so the margins are real, and each one says what it is protecting against. Re-measure and raise deliberately, in a reviewed change, rather than nudging a number to make CI green.",
  "budgets": [
    {
      "id": "client-total-bytes",
      "label": "Total client JavaScript and CSS shipped by the build",
      "measured": 2197775,
      "limit": 2600000,
      "marginPercent": 18,
      "reasoning": "The whole client payload today is 2.15 MiB across JS and CSS. The curriculum is compiled into the client bundle and is actively growing as content lands, so the margin has to absorb ordinary authoring without a budget change: roughly 400 KiB of headroom, which is about a fifth again of the current total. Anything that consumes that in one change is a dependency or an architecture decision, not a content edit, and deserves the conversation this gate forces."
    },
    {
      "id": "client-css-bytes",
      "label": "Total client CSS",
      "measured": 234546,
      "limit": 280000,
      "marginPercent": 27,
      "reasoning": "CSS is 229.0 KiB on disk, but that is not what a visitor downloads. A page loads the core sheet (129,080 bytes) plus the selected theme's tokens and components and the selected layout: 157,000 bytes on the critical path today. The other five theme bundles and two layout bundles are published so a deployment can switch to them, and are never fetched by anyone who does not. Re-measured after syncing Gallery 011fd08: the five unselected bundles were 66-75 byte stubs under the previous pin and are now complete treatments, which moved this number by 57,158 bytes while moving the critical path by 5,821. The limit is raised to cover the published set with room for a seventh theme, because a theme this deployment does not select must not be able to fail its build. What this gate protects against - a second framework, or the theme boundary leaking back into core - lands in the core sheet, and the 27% margin still fails loudly on a step change there."
    },
    {
      "id": "largest-chunk-bytes",
      "label": "Largest single JavaScript chunk",
      "measured": 1305557,
      "limit": 1600000,
      "marginPercent": 22,
      "reasoning": "One chunk carries the compiled platform package and curriculum at 1.24 MiB, which is 64% of all client JavaScript. It is the single most important number on this page because it is what a first visit must download before anything is interactive, and it is the one most likely to grow silently as content is added. The 22% margin leaves room for the content that is already planned while making a step change - a new dependency pulled into that chunk, or code-splitting quietly regressing - fail loudly."
    }
  ],
  "vitals": [
    {
      "id": "home-lcp-ms",
      "label": "Largest Contentful Paint on the home route",
      "limit": 4000,
      "reasoning": "Measured in the browser suite against the production build on a local server, so this is a ceiling rather than a field measurement: it cannot see real networks or real devices. 4000 ms is the Core Web Vitals 'poor' boundary. On a loopback server the honest expectation is a small fraction of that, so anything approaching this limit means a render-blocking regression rather than ordinary variance. Deliberately generous: a vitals gate that flakes gets disabled, and a disabled gate measures nothing. Lighthouse and a real throttled profile need full Chrome, which CI does not install (it fetches the headless shell only), so that is not what this checks."
    }
  ]
}
